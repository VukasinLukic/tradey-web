# Server Configuration
PORT=5000
NODE_ENV=production

# Firebase Project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# CORS Configuration (set to your frontend URL)
CORS_ORIGIN=https://your-frontend-domain.railway.app

# Admin Dashboard Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-here

# Firebase Admin SDK Service Account (REQUIRED for Railway)
# In Railway, set this environment variable to the entire JSON content of your firebase-service-account.json
# Copy the entire JSON object (including curly braces) as a single-line string
# Example: {"type":"service_account","project_id":"...","private_key_id":"...","private_key":"..."}
FIREBASE_SERVICE_ACCOUNT=

# Alternative: Local file path (only for local development with Docker)
# FIREBASE_ADMIN_KEY_PATH=./firebase-service-account.json
